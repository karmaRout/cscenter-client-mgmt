{
  "name": "cscenter-client-mgmt",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../backend/frontend-build && cp -r dist ../backend/frontend-build && cd ../backend && npm install",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  }
}
